# fix checklist (GTM rollback)

Все изменения премиум-редизайна GTM этапов/задач возвращены к исходной версии. Чек-лист сброшен для повторной реализации при необходимости.

## 0. Большая идея: "GTM Studio"
- [ ] Страница оформлена как рабочая студия с акцентом на этапы-главы, задачи-сцены и подзадачи-чек-листы.
- [ ] Ключевая информация в Cards-режиме видна сразу, второстепенная раскрывается по требованию, есть режим фокуса на этап.

## 1. Новая структура Cards-режима (этапы)
- [ ] Header этапа собран в одну строку с тремя зонами: индекс + название, метрики-чипы, иконки действий.
- [ ] Убраны крупные вложенные карточки в header; метрики сведены в чипы.
- [ ] Часть чипов скрыта и появляется при hover/фокусе.

### 1.2. Прогресс этапа — тонкая "линия жизни"
- [ ] В header оставлен только % прогресса.
- [ ] Под header добавлена тонкая полоса прогресса с мутным видом при 100% и скрытием при 0%.

### 1.3. "Focus mode" для этапа
- [ ] Добавлена кнопка "🎯 Фокус" в действиях этапа.
- [ ] В фокус-режиме остаётся только выбранный этап и панель возврата ко всем этапам.
- [ ] Режим сохраняется до выхода.

## 2. Cards-режим (задачи)
- [ ] Задача отображается одной компактной строкой с чекбоксом статуса, названием, вторичкой и иконками действий.
- [ ] Заголовок задачи в одну строку, вторичка содержит срок, важность/срочность (если есть), прогресс подзадач и комментарии.

### 2.2. Подзадачи — чеклист
- [ ] Подзадачи представлены строками чекбоксов без лишних рамок, с inline-редактированием и действиями при hover.
- [ ] Добавление подзадачи — инлайн-строка с полем на всю ширину и кнопкой `+` справа.

### 2.3. Авто-свёртка пустого
- [ ] При отсутствии подзадач `task-body` скрыт, в мета отображается `0/0`, прогресс-бар показывается только при наличии подзадач.

## 3. Table-режим (этапы) — "премиальный Excel"
- [ ] Таблица плотная (≈44px строки), действия вынесены в иконки, план/факт компактно в две строки, статус и риск в чипах.
- [ ] Поддержано inline-редактирование статуса, плановых дат и риска с мини-toolbar `✓/✕` в строке.

## 4. "Компактный список задач" (таблица задач)
- [ ] Каждая строка = одна задача: жирное название + строка вторички (этап + срок) в той же ячейке, прогресс подзадач текстом, компактные иконки действий.
- [ ] Добавлены быстрые фильтры-чипы: Все/ToDo/В работе/Готово, С риском, Без срока, мгновенно влияющие на таблицу.

## 5. Микро-анимации и ощущение дорогого продукта
- [ ] Плавные раскрытия этапов/задач, hover-подсветка строк, лёгкая анимация прогресса, skeleton-loading для этапов/задач.

## 6. Единый стиль (CSS tokens)
- [ ] Введены/расширены токены в `:root` для поверхностей, границ, текста, акцента, рисков и радиусов.
- [ ] Ключевые блоки (stage-card, pill, task-row и др.) используют токены, подчёркивая иерархию и компактность.

## 7. Логика раскрытий + запоминание состояния
- [ ] Этапы и задачи по умолчанию свернуты, переключение через `stage-collapse`/кнопки задач.
- [ ] Состояния свёртки, фокуса и фильтров сохраняются в localStorage и восстанавливаются при загрузке.

## 8. Приёмка
- [ ] Cards-этапы помещаются в экран в свернутом виде, ключевая информация сразу в header.
- [ ] Cards-задачи выглядят списком, подзадачи — чеклист без вложенных рамок.
- [ ] Table-режим плотный и быстрый, как премиальный spreadsheet.
- [ ] Компактные задачи плотные, с сильной иерархией и быстрыми фильтрами; Канбан сохранён.
- [ ] Режимы, плотность и раскрытия запоминаются.
