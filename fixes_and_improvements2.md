Отмечай выполненные пункты, заменяя `[ ]` на `[x]`.

---

## 1. Дашборд и метрики

- [ ] Починить дашборд:
  - восстановить загрузку и отображение данных (сейчас всё пустое/сломанное);
  - привести разметку и отступы в порядок (ничего не должно «плавать»).

- [ ] Переработать визуальный вид дашборда
  - сделать блоки более структурированными и визуально чистыми;
  - выровнять сетку, отступы, заголовки, подписи.

- [ ] Добавить ключевые KPI и метрики
  - количество активных, закрытых и архивных проектов;
  - количество проектов по брендам;
  - распределение проектов по статусу/этапам GTM;
  - количество проектов с просроченными этапами/задачами;
  - количество активных продуктовых групп и групп с «проблемными» проектами.

- [ ] Сделать визуализацию KPI
  - прогресс-бары/мини-диаграммы:
    - доля завершённых проектов;
    - доля проектов с просрочками;
    - прогресс по GTM у ключевых проектов;
  - блок «Риски» с кратким списком самых проблемных проектов.

- [ ] Улучшить блок ближайших дедлайнов
  - отображать ближайшие GTM-этапы и задачи с датой и проектом;
  - визуально выделять просроченные позиции.

- [ ] Сделать дашборд кликабельным
  - переходы в проекты/группы по клику на строки/карточки;
  - быстрые фильтры по бренду, группе и статусу проекта.

---

## 2. Навигация, меню и логотип

- [x] Сделать глобальное меню на всех страницах и подстраницах
  - одинаковый header с меню для дашборда, групп, проектов, карточек и раздела шаблонов;
  - пункты меню: например, «Дашборд», «Группы», «Проекты», «Шаблоны», «Настройки».

- [x] Убрать хлебные крошки
  - при наличии постоянного верхнего меню breadcrumbs больше не нужны.

- [x] Настроить favicon и логотип
  - сделать логотип таким же, как favicon (единый знак);
  - по клику на логотип возвращать на главную (дашборд).

- [x] Убрать пустую «карточку проекта» из меню
  - если в меню есть нерабочий/пустой пункт, убрать его или сделать функциональным.

---

## 3. Страница проектов, групп и быстрый просмотр

- [ ] Страница «Проекты»
  - отобразить проекты в виде карточек по **4 в ряд**;
  - карточка проекта:
    - слева: обложка проекта (картинка),
    - справа: название, группа, бренд, рынок, GTM, плановая дата, приоритет;
    - прогресс-бар проекта (по GTM/задачам);
    - дата создания;
    - кнопка «Перейти в проект» и «Быстрый просмотр».

- [ ] Страница «Продуктовые группы»
  - отображать группы карточками по **3 в ряд**;
  - карточка группы:
    - обложка группы,
    - название,
    - статистика по проектам (активные/закрытые/архивные, риски),
    - статус-бар «здоровья» группы;
    - кнопка «Перейти в группу».

- [ ] После создания проекта сразу переходить в карточку проекта
  - не оставаться на форме/списке, а открывать страницу созданного проекта.

- [ ] Быстрый просмотр (popup) проекта
  - переработать внешний вид popup:
    - убрать «прилипание», задать отступы от краёв;
    - сделать аккуратную карточку поверх затемнённого фона;
  - все поля внутри popup должны быть выровнены и иметь нормальные отступы.

- [ ] «Показать таблицей» для проектов/групп
  - в табличном режиме:
    - для групп — возможность открыть группу (строка кликабельна и/или отдельная кнопка);
    - для проектов — возможность открыть проект или быстрый просмотр;
  - табличный режим не должен ломать вёрстку (ничего не вылазит за границы).

- [ ] Карточки проектов в группе
  - привести карточки к ровной сетке, чтобы элементы не «вылетали» за рамки;
  - обеспечить при карточках и в табличном режиме переход в проект.

---

## 4. Фильтры, чекбоксы и попапы

- [ ] Фильтры на страницах проектов
  - визуально разделить фильтры (отступы, группировка);
  - сделать так, чтобы поля не налипали друг на друга;
  - вынести кнопку **«Применить фильтры»** в отдельный, хорошо заметный элемент.

- [ ] Фильтры внутри карточки проекта
  - при наличии фильтров (задачи, этапы и т.д.) — также разделить поля;
  - уменьшить визуальный вес чекбоксов, сделать их аккуратнее и современнее;
  - обеспечить ровные отступы и выравнивание.

- [ ] Привести все попапы к единому стандарту
  - во всех pop-up окнах:
    - поля ввода должны быть логично сгруппированы и отделены отступами;
    - чекбоксы — аккуратные, не громоздкие;
    - заголовок, тело и кнопки выровнены;
  - единый стиль кнопок (основная/второстепенная) внизу popup.

---

## 5. Карточка проекта: шапка, поля и прогресс

- [x] Добавить новые параметры в проект (создание и шапка)
  - поля:
    - **MOQ**;
    - **Promo (Price)**;
    - **RRP (Price)**;
    - **FOB цена**;
  - эти поля должны быть:
    - доступны при создании проекта;
    - отображаться и редактироваться в шапке карточки проекта слева.

- [ ] Выровнять обложку проекта в карточке
  - обложка не должна быть «съехавшей» относительно контейнера;
  - задать стабильный размер и пропорции, аккуратные отступы.

- [ ] В шапке проекта
  - слева: название, группа, бренд, рынок, статус, плановая/фактическая дата, основные цены (MOQ, Promo, RRP, FOB);
  - справа: обложка проекта (с кнопками загрузки/замены/снятия).

- [ ] Статус-бар проекта в шапке
  - отображать общий прогресс по проекту:
    - например, процент завершённых GTM-этапов или задач;
  - индикатор риска (если есть просроченные этапы/задачи).

- [x] Добавить короткий цифровой ID проекта
  - генерировать и хранить короткий цифровой ID (при этом UUID остаётся внутренним);
  - отображать ID в левом блоке под названием проекта;
  - добавить кнопку «Скопировать ID»:
    - по клику копировать ID в буфер обмена;
    - показывать краткий фидбек («ID скопирован»).

---

## 6. GTM-этапы, задачи и подзадачи

- [ ] Исправить логику GTM-этапов
  - GTM-этап — верхний уровень:
    - внутри этапа — задачи;
    - внутри задач — подзадачи;
  - выполнение GTM-этапа должно зависеть от выполнения задач/подзадач:
    - прогресс этапа = агрегированный прогресс задач;
    - прогресс проекта = агрегированный прогресс всех этапов.

- [ ] Связать задачи/подзадачи с GTM-этапами
  - каждая задача должна быть привязана к конкретному GTM-этапу;
  - подзадачи — к задаче;
  - изменения статусов задач/подзадач должны автоматически обновлять прогресс этапа и проекта.

- [ ] Шаблоны GTM-этапов с задачами и подзадачами
  - при сохранении шаблона GTM:
    - сохранять структуру этапов **вместе** с их задачами и подзадачами;
  - при применении шаблона:
    - создавать этапы, задачи и подзадачи в новом проекте по образцу.

- [ ] Визуальный UX задач и подзадач
  - сделать подзадачи инлайн под задачей (без лишних модальных окон);
  - подзадачи — с чекбоксами и отступами, читаемые и аккуратные;
  - прогресс задачи считать по подзадачам и показывать мини-прогресс-бар.

- [ ] Ввод комментариев к задачам
  - ввод комментариев инлайн (не только через модальное окно);
  - добавить лёгкий текстовый редактор (жирный/курсив/списки/ссылки);
  - возможность редактировать и удалять комментарии (с меткой «изменён», если нужно).

---

## 7. Характеристики продукта (RU/EN)

- [ ] Перенести характеристики на отдельную страницу
  - в шапке проекта, под описанием, добавить кнопку «Характеристики»;
  - по клику открывать отдельную страницу/вкладку характеристик, чтобы не перегружать основную карточку.

- [ ] Инлайн-редактирование характеристик
  - значения редактируются прямо в таблице (без модального окна);
  - обеспечить автосохранение или понятный механизм сохранения;
  - сохранить отображение Label/Value RU и EN рядом.

- [ ] Структура секций и полей
  - оставить возможность добавлять/переименовывать/удалять секции и поля;
  - порядок секций и полей можно менять;
  - шаблоны характеристик по-прежнему работают через отдельный список шаблонов.

---

## 8. Галерея и обложка

- [ ] Сделать галерею Grid-сеткой
  - отображать изображения сеткой по **5 штук в ряд** (с адаптацией под ширину экрана);
  - подписывать изображения аккуратно и читаемо.

- [ ] Добавить лайтбокс
  - при клике на изображение открывать его в полноэкранном/большом окне;
  - дать возможность листать изображения стрелками/кнопками.

- [ ] Добавить кнопку «Скачать все»
  - реализовать кнопку «Скачать все как архив»:
    - собрать все изображения проекта в один архив и отдать на скачивание.

- [ ] Починить кнопку «Снять обложку»
  - при нажатии:
    - убрать текущее изображение из роли обложки;
    - в UI отобразить, что обложка не задана (или выбрать дефолтную).

---

## 9. Шаблоны (GTM и характеристики)

- [x] Добавить раздел «Шаблоны» в меню
  - отдельная страница/раздел для управления шаблонами:
    - шаблоны GTM-этапов (+ задач и подзадач);
    - шаблоны характеристик.

- [x] Список и редактирование шаблонов
  - отображать все шаблоны списком;
  - дать возможность:
    - переименовывать шаблон;
    - редактировать его структуру;
    - удалять шаблон с подтверждением;
    - создавать новый шаблон без привязки к конкретному проекту (по возможности).

---

## 10. Backend: ошибки и стабильность

- [ ] Исправить ошибки, связанные с `GTMStage` и полем `name`
  - в `create_gtm_stage` заменить `created.name` на корректное поле (например, `title`) или добавить поле `name` в модель и репозиторий;
  - в `update_gtm_stage` заменить `existing.name` на корректное поле или использовать новое поле;
  - убедиться, что логирование `log_event` больше не вызывает `AttributeError` и 500 ошибок.

- [ ] Проверить работу всех GTM-эндпоинтов
  - создать/обновить/удалить GTM-этап;
  - сохранить шаблон этапов;
  - применить шаблон к проекту;
  - проверить историю изменений.

- [ ] Провести регрессионное тестирование основных сценариев
  - группы: создание/редактирование/архивирование/удаление;
  - проекты: создание/редактирование/архивирование/удаление;
  - GTM: этапы, шаблоны, прогресс;
  - задачи и подзадачи;
  - характеристики;
  - файлы и изображения (загрузка/скачивание/удаление);
  - галерея и обложка;
  - комментарии и история изменений;
  - дашборд, шаблоны, навигация через меню и быстрый просмотр.
