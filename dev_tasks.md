# Haier Project Tracker — Dev Tasks

Чек-лист задач для реализации проекта согласно `Haier_Project_Tracker_TZ.md`.  
Отмечай выполненные пункты `[x]`.

---

## 1. Базовая подготовка

- [x] Создать/обновить файл `Haier_Project_Tracker_TZ.md` в корне репозитория и убедиться, что он актуален.
- [x] Создать `AGENTS.md` (этот файл) в корне репозитория.
- [x] Создать `project-plan.md` (план проекта).
- [x] Создать `dev_tasks.md` (данный чек-лист).
- [x] Настроить базовую структуру проекта (директории, минимальные файлы запуска приложения).

---

## 2. Модель данных и архитектура

- [x] Описать сущности и связи в виде текстовой схемы (ER-диаграмма или эквивалент):
  - Продуктовая группа;
  - Проект;
  - GTM-этап;
  - Задача;
  - Подзадача;
  - Характеристика (секции и поля);
  - Шаблон GTM;
  - Шаблон характеристик;
  - Файл;
  - Изображение;
  - Комментарий;
  - История изменений.

- [x] Определить связи между сущностями:
  - Продуктовая группа ↔ Проекты (1 ко многим);
  - Проект ↔ GTM-этапы (1 ко многим);
  - Проект ↔ Задачи (1 ко многим);
  - Задача ↔ Подзадачи (1 ко многим);
  - Проект ↔ Секции характеристик (1 ко многим);
  - Секция ↔ Поля (1 ко многим);
  - Проект ↔ Файлы (1 ко многим);
  - Проект ↔ Изображения (1 ко многим);
  - Проект ↔ Комментарии (1 ко многим);
  - Проект ↔ История изменений (1 ко многим).

- [ ] Спроектировать слой данных (хранение в одном локальном хранилище/файле).

---

## 3. Запуск приложения

- [ ] Реализовать базовый запуск приложения как локального веб-приложения.
- [ ] Обеспечить открытие приложения через ссылку в браузере (локальный адрес).
- [ ] Настроить запуск через иконку/ярлык (одна команда/исполняемый файл, который поднимает приложение и открывает браузер).

---

## 4. Дашборд

- [ ] Реализовать главную страницу «Дашборд».
- [ ] Отображать сводку по статусам проектов (Активный / Закрыт / Архивирован).
- [ ] Отображать сводку по продуктовым группам:
  - название;
  - количество активных проектов;
  - индикатор рисков (если есть просрочки).
- [ ] Реализовать список ближайших важных дат:
  - дедлайны GTM-этапов;
  - дедлайны задач.
- [ ] Реализовать ленту последних изменений.
- [ ] Реализовать переходы по клику:
  - в продуктовую группу;
  - в проект.
- [ ] Реализовать фильтры на дашборде:
  - по группе;
  - по бренду;
  - по статусу проекта.
- [ ] Добавить контекстные меню по правой кнопке мыши для карточек групп и проектов на дашборде.

---

## 5. Продуктовые группы

- [ ] Реализовать модель «Продуктовая группа» с полями:
  - название;
  - описание;
  - статус (Активна / Архивирована);
  - бренды;
  - дополнительные пользовательские поля.

- [ ] Реализовать экран списка продуктовых групп:
  - отображение всех групп (кроме архивных по умолчанию);
  - отображение краткой сводки.

- [ ] Реализовать создание новой группы.
- [ ] Реализовать редактирование группы.
- [ ] Реализовать архивирование и восстановление группы.
- [ ] Реализовать фильтры:
  - по статусу (включая «Показывать архив»);
  - по бренду;
  - по пользовательским полям (если есть).

- [ ] Реализовать удаление группы с подтверждением:
  - определить и реализовать желаемое поведение с привязанными проектами (запрет удаления при наличии проектов, либо каскадное удаление/перенос — согласно решению).

- [ ] Добавить контекстное меню по правой кнопке мыши на группе:
  - открыть;
  - редактировать;
  - архивировать / восстановить;
  - удалить.

---

## 6. Проекты

- [ ] Реализовать модель «Проект» с полями из ТЗ:
  - название;
  - группа;
  - бренд;
  - рынок/регион;
  - краткое и расширенное описание;
  - статус (Активный / Закрыт / Архивирован);
  - текущий GTM-этап;
  - плановая и фактическая дата запуска;
  - приоритет;
  - пользовательские поля.

- [ ] Реализовать создание проекта внутри выбранной группы.
- [ ] Реализовать редактирование проекта.
- [ ] Реализовать изменение статусов:
  - Активный;
  - Закрыт;
  - Архивирован.

- [ ] Реализовать список проектов в группе:
  - вид «карточки»;
  - вид «таблица».

- [ ] Реализовать фильтры по проектам:
  - по бренду;
  - по статусу;
  - по текущему GTM-этапу;
  - по периоду запуска.

- [ ] Реализовать архивирование и восстановление проектов.
- [ ] Реализовать удаление проекта с подтверждением:
  - удалять связанные GTM-этапы, задачи, подзадачи, характеристики, файлы, изображения, комментарии, записи истории.

- [ ] Реализовать контекстное меню по правой кнопке мыши на проекте:
  - открыть;
  - редактировать;
  - закрыть;
  - архивировать / восстановить;
  - дублировать (если нужно);
  - удалить.

---

## 7. Go-to-market (GTM) этапы

- [ ] Реализовать модель GTM-этапа (поля: название, описание, порядок, плановые даты начала/окончания, фактическая дата завершения, статус, риск, чек-лист).
- [ ] Реализовать интерфейс управления этапами в карточке проекта:
  - список этапов;
  - отображение статусов и дат.

- [ ] Реализовать добавление GTM-этапа.
- [ ] Реализовать редактирование GTM-этапа.
- [ ] Реализовать изменение порядка этапов.
- [ ] Реализовать чек-лист внутри этапа:
  - добавление пунктов;
  - отметка «выполнено»;
  - удаление пунктов.
- [ ] Реализовать установку статусов (Не начат / В работе / Завершён / Отменён).
- [ ] Реализовать отметку риска (ручная или автоматически по просрочке).
- [ ] Реализовать удаление этапа с подтверждением.

- [ ] Реализовать работу с шаблонами GTM:
  - сохранение текущей структуры этапов проекта как шаблона;
  - выбор шаблона при создании проекта;
  - при необходимости — применение шаблона к существующему проекту (аккуратная логика объединения/замены).

- [ ] Реализовать экспорт структуры GTM-этапов в Excel.
- [ ] Реализовать импорт структуры GTM-этапов из Excel с валидацией.

- [ ] Добавить контекстное меню по правой кнопке мыши для этапов:
  - редактировать;
  - управлять чек-листом;
  - дублировать;
  - удалить.

---

## 8. Характеристики продукта (RU/EN)

- [ ] Реализовать структуру секций характеристик, связанных с проектом.
- [ ] Реализовать поля характеристик с:
  - Label RU;
  - Label EN;
  - Value RU;
  - Value EN;
  - типом поля (текст, число, список, флажок и т.д.).

- [ ] Реализовать интерфейс, где RU и EN значения отображаются рядом.

- [ ] Реализовать операции с секциями:
  - добавление;
  - переименование;
  - изменение порядка;
  - удаление (с подтверждением).

- [ ] Реализовать операции с полями:
  - добавление;
  - редактирование Label RU/EN, типа;
  - изменение порядка;
  - удаление (с подтверждением).

- [ ] Реализовать шаблоны характеристик:
  - создание шаблона (структура секций и полей без значений);
  - выбор шаблона при создании проекта;
  - копирование структуры из другого проекта.

- [ ] Реализовать экспорт характеристик проекта в Excel:
  - секция;
  - Label RU;
  - Label EN;
  - Value RU;
  - Value EN;
  - тип (опционально).

- [ ] Реализовать импорт характеристик из Excel:
  - сопоставление секции и полей по Label RU/EN;
  - заполнение Value RU/EN;
  - обработка ошибок сопоставления с понятными сообщениями.

- [ ] Добавить контекстное меню по правой кнопке мыши для секций и полей:
  - редактировать;
  - дублировать (при необходимости);
  - удалить.

---

## 9. Задачи, подзадачи и комментарии

- [ ] Реализовать модель «Задача» с полями:
  - заголовок;
  - описание;
  - статус;
  - срок;
  - важность;
  - привязка к GTM-этапу.

- [ ] Реализовать модель «Подзадача»:
  - текст;
  - флаг выполнения.

- [ ] Реализовать представление задач:
  - список;
  - доска по статусам.

- [ ] Реализовать фильтры по задачам:
  - по статусу;
  - по срокам (ближайшие, просроченные);
  - по GTM-этапу.

- [ ] Реализовать CRUD-операции задач:
  - создание;
  - редактирование;
  - смена статуса;
  - дублирование (если нужно);
  - удаление с подтверждением.

- [ ] Реализовать CRUD-операции подзадач:
  - добавление;
  - редактирование;
  - отметка выполнено/не выполнено;
  - удаление с подтверждением.

- [ ] Реализовать комментарии к задачам:
  - добавление;
  - отображение;
  - при необходимости — редактирование/удаление.

- [ ] Добавить контекстное меню по правой кнопке мыши для задач и подзадач:
  - открыть/редактировать;
  - изменить статус (где применимо);
  - дублировать (если нужно);
  - удалить.

---

## 10. Галерея и обложка

- [ ] Реализовать модель «Изображение проекта».
- [ ] Реализовать интерфейс галереи в карточке проекта:
  - список/плитка изображений с подписями.

- [ ] Реализовать загрузку изображений.
- [ ] Реализовать редактирование подписи/комментария.
- [ ] Реализовать изменение порядка изображений (по возможности).
- [ ] Реализовать выбор обложки из галереи.
- [ ] Обеспечить, чтобы обложка была квадратной или корректно кадрировалась.
- [ ] Реализовать удаление изображения с подтверждением.

- [ ] Добавить контекстное меню по правой кнопке мыши:
  - просмотреть;
  - назначить обложкой;
  - изменить подпись;
  - удалить.

---

## 11. Файлы и документы

- [ ] Реализовать модель «Файл проекта».
- [ ] Реализовать интерфейс списка файлов в проекте:
  - имя файла;
  - описание;
  - категория;
  - дата.

- [ ] Реализовать загрузку файла.
- [ ] Реализовать редактирование описания и категории.
- [ ] Реализовать скачивание файла.
- [ ] Реализовать удаление файла с подтверждением.

- [ ] Добавить контекстное меню по правой кнопке мыши:
  - скачать;
  - изменить описание/категорию;
  - удалить.

---

## 12. Комментарии к проекту и история изменений

- [ ] Реализовать комментарии к проекту:
  - добавление;
  - отображение;
  - при необходимости — редактирование/удаление.

- [ ] Определить набор событий для истории изменений (согласно ТЗ).
- [ ] Реализовать запись событий в историю:
  - изменения статусов;
  - работа с GTM-этапами;
  - задачи/подзадачи;
  - изменения характеристик (минимум укрупнённо);
  - операции с файлами и изображениями;
  - смена обложки.

- [ ] Реализовать отображение истории в карточке проекта (лента).

---

## 13. Архивирование и удаление

- [ ] Проверить, что у проектов реализованы:
  - статусы Активный / Закрыт / Архивирован;
  - фильтрация по статусам;
  - архивирование/восстановление;
  - удаление с подтверждением.

- [ ] Проверить, что у продуктовых групп реализованы:
  - статусы Активна / Архивирована;
  - фильтрация с опцией «Показывать архив»;
  - архивирование/восстановление;
  - удаление с подтверждением (с корректной обработкой проектов).

- [ ] Проверить, что у задач:
  - статус «Готово» воспринимается как закрыта;
  - есть фильтр «скрыть выполненные» (если реализуется);
  - есть возможность удалить задачу.

- [ ] Убедиться, что во всех сущностях, где указано удаление в ТЗ, оно реализовано с подтверждением.

---

## 14. Импорт/экспорт

- [ ] Реализовать экспорт характеристик проекта в Excel.
- [ ] Реализовать импорт характеристик из Excel с сопоставлением по секции и Label RU/EN.
- [ ] Реализовать экспорт структуры GTM-этапов в Excel.
- [ ] Реализовать импорт структуры GTM-этапов из Excel.
- [ ] Реализовать экспорт списка проектов (с ключевыми полями) в Excel.
- [ ] Для каждого сценария импорта реализовать обработку ошибок с понятным сообщением.

---

## 15. Резервное копирование

- [ ] Реализовать функцию «Сохранить резервную копию базы» (экспорт всех данных в файл).
- [ ] Реализовать функцию «Восстановить из резервной копии» (загрузка файла).
- [ ] Задокументировать путь к основному файлу данных для ручного резервного копирования.

---

## 16. Финальная проверка по ТЗ

- [ ] Пройтись по `Haier_Project_Tracker_TZ.md` пункт за пунктом.
- [ ] Для каждого раздела ТЗ убедиться, что соответствующий функционал реализован.
- [ ] Если что-то не реализовано — явно зафиксировать, почему и какое решение предлагается (изменить ТЗ или добавить задачу).
- [ ] Прогнать основные пользовательские сценарии:
  - добавление групп/проектов;
  - настройка GTM;
  - задачи/подзадачи;
  - характеристики RU/EN;
  - галерея и обложка;
  - файлы и комментарии;
  - история изменений;
  - архивирование/удаление;
  - импорт/экспорт;
  - резервные копии.

- [ ] Зафиксировать версию как v1 (рабочая боевая версия).

