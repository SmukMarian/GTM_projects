# Codex Task Pack — UI upgrade for 2 pages (GTM table + Compact tasks)
Отмечай выполненные пункты, заменяя `[ ]` на `[x]`.

Цель: на странице проекта привести **GTM-этапы (режим Таблица)** и **Компактный список задач (режим Таблица)** к уровню топ‑продукта: плотный сканируемый вид, редактирование “on‑demand”, нормальная адаптивность.  
Ограничение: **не ломаем архитектуру проекта** — не меняем API, модели данных, имена ключей, существующие DOM id/классы. Только аддитивные изменения (классы, обёртки, стили, улучшение рендера).

---

## 0. Общие правила
- [x] Не удалять существующие id/классы и не переименовывать публичные поля данных.
- [x] Любой новый UI добавлять через новые классы/контейнеры (например `.gtm-table--view`, `.gtm-table--edit`).
- [x] Глобальные element‑селекторы для контролов НЕ использовать. Только классы.

---

## 1. GTM-этапы — режим **Таблица** (страница проекта)

### 1.1. Двухрежимные строки: **View по умолчанию + Edit по клику**
Сейчас строка = мини‑форма → громоздко.

- [x] В рендере таблицы этапов (функция/блок, где строится `<tr>` этапа в `frontend/project.html`) сделать 2 слоя:
  - `.stage-row-view` — видимый по умолчанию,
  - `.stage-row-edit` — скрыт по умолчанию.
- [x] В `.stage-row-view` показывать **только текст + чипы**:
  - План: `План: DD.MM.YY → DD.MM.YY` (1 строка),
  - Факт: `Факт: —` (2 строка, muted),
  - Статус — чип,
  - Риск — иконка/чип,
  - Прогресс — мини‑бар 4px + `%` рядом,
  - Задачи — `Готово X/Y` + подзадачи `A/B`.
- [x] В `.stage-row-edit` держать текущие инпуты (datapicker, select, чекбокс), **без изменения логики сохранения**.
- [x] Кнопка ✏️ в колонке действий:
  - переключает только эту строку в edit‑режим;
  - при edit показывает ✅ (save) и ✖️ (cancel) вместо ✏️.

**Технически (JS):**
- [x] Добавить функции:
  - `enterStageEdit(stageId)`
  - `exitStageEdit(stageId, save=false)`
- [x] Реализовать переключение через `data-stage-id`:
  - `.stage-row-view[data-stage-id="..."] { display: ... }`
  - `.stage-row-edit[data-stage-id="..."] { display: none/block }`
- [x] При `save=false` восстановить значения из текущего state (без запроса к API).

---

### 1.2. Сжать строки и привести таблицу к “сканируемому” виду
- [x] В CSS таблицы этапов:
  - `table-layout: fixed; width: 100%;`
  - высота строки 44–48px в view‑режиме.
- [x] У ячеек:
  - `vertical-align: top;`
  - `padding: 6px 8px;`
  - `min-width: 0;`
- [x] Для длинных названий этапа:
  - разрешить перенос до 2 строк, дальше `ellipsis`.

### 1.3. Баланс колонок + sticky‑колонки
- [x] Зафиксировать ширины:
  - `#` — 44px
  - `Этап` — min 240px
  - `Сроки` — min 280px
  - `SLA/риск` — 160px
  - `Статус` — 140px
  - `Прогресс` — 120px
  - `Задачи` — 140px
  - `Комментарии` — 110px
  - `Действия` — 150px
- [x] Сделать sticky для 2 первых колонок (`#` и `Этап`) чтобы горизонтальный скролл не ломал работу.

### 1.4. Прогресс этапа — мини‑линия, не отдельный блок
- [x] В view‑режиме заменить текущий толстый прогресс на:
  - бар 4px,
  - текст `%` справа маленьким шрифтом.
- [x] Если прогресс 0 — скрывать бар, оставлять `0%` muted.
- [x] Если 100 — бар muted (не акцент).

### 1.5. Действия — только иконки, без раздувания
- [x] В колонке действий оставить только:
  - ✏️ / ✅ / ✖️
  - ➕ (add task)
  - 💬 (comments)
- [x] Остальные редкие действия оставить в контекстном меню (три точки) — **если оно уже есть**; если нет — не добавлять, чтобы не ломать UX.

### 1.6. Панель управления сверху — компактные группы
- [x] Сгруппировать кнопки:
  1) **Основные**: `Добавить этап`, `Шаблон (select)`, `Применить`, `Сохранить как шаблон`.
  2) **Сервис** (справа): `Импорт Excel`, `Экспорт Excel`.
- [x] Select шаблонов ограничить шириной 420–520px, не растягивать на весь экран.

### 1.7. Hover‑подсветка строки
- [x] Добавить лёгкую подложку на hover строки view‑режима.

---

## 2. Компактный список задач — режим **Таблица** (страница проекта)

### 2.1. Сводка сверху — сделать компактной/сворачиваемой
- [x] Превратить 4 карточки сводки в **одну компактную строку** с чипами:
  - `В выборке: 24`
  - `Готово: 8`
  - `Проср.: 0`
  - `Прогресс: 30%`
- [x] Добавить кнопку `Сводка ▾` (по умолчанию свернута на экранах < 900px).

### 2.2. Sticky‑панель фильтров/поиска над задачами
- [x] Над таблицей задач добавить sticky‑ряд:
  - поиск по названию (client‑side),
  - чипы статуса `Все / ToDo / В работе / Готово`,
  - чипы `С риском`, `Без срока`,
  - сортировка (select) `по сроку / по важности / по этапу`.
- [x] Это не меняет API: фильтруем текущий массив задач в JS.

### 2.3. Уплотнить строки задач и усилить иерархию
- [x] Высота строки задач 40–44px.
- [x] В колонке “Задача”:
  - 1 строка — название (bold),
  - 2 строка (мелко muted) — этап + срок.
- [x] Прогресс подзадач показывать **только текстом** `2/5` (без больших полос в таблице).
- [x] Действия — только иконки (edit / comments / timer / delete), меньше размер.

### 2.4. Синхронизация визуала с Канбаном
- [x] Применить те же токены цветов/радиусов/теней к карточкам канбана:
  - `border-radius: 14–16px`,
  - `box-shadow: var(--shadow-soft)`,
  - те же `StatusChip`.

---

## 3. Acceptance (приёмка)

### GTM-таблица
- [x] По умолчанию таблица сканируется как список: инпуты не видны до edit.
- [x] Высота строк уменьшилась минимум в 2 раза против текущей.
- [x] Названия этапов читаются без «ломания» по 5 строк.
- [x] Колонка действий не раздувает таблицу, все действия — иконками.
- [x] Sticky‑колонки работают и не конфликтуют со стилями.

### Компактные задачи
- [x] Сводка не занимает больше 1 строки (или свернута).
- [x] Фильтры/поиск доступны всегда (sticky).
- [x] Таблица задач плотная, лёгкая для массовой работы.
- [x] Канбан визуально не выбивается из общего стиля.

---
